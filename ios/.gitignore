# Generated by XcodeGen — never commit. Regenerate on the Mac with `xcodegen generate`.
*.xcodeproj/

# Xcode build outputs + user state.
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/

# Swift Package Manager resolved deps.
Package.resolved

# CocoaPods (we don't use it but just in case someone adds it).
Pods/

# macOS junk.
.DS_Store
