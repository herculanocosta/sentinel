# Generated by XcodeGen. We DO commit it so Mac users can just extract & double-click.
# Regenerate after editing project.yml with `xcodegen generate` (or use the included setup.sh).
# *.xcodeproj/

# Xcode build outputs + user state.
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/

# Swift Package Manager resolved deps.
Package.resolved

# CocoaPods (we don't use it but just in case someone adds it).
Pods/

# macOS junk.
.DS_Store
